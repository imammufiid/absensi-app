<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    tools:context=".ui.detailattendance.DetailAttendanceActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <!-- Header -->
        <!-- Button Back -->
        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:background="@null"
            android:contentDescription="@string/icon_back"
            android:paddingHorizontal="20dp"
            android:paddingVertical="20dp"
            android:src="@drawable/ic_left" />

        <!-- Content -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/btn_back"
            android:background="@drawable/bg_page_white"
            android:orientation="vertical"
            android:paddingHorizontal="20dp"
            android:paddingTop="20dp">

            <include
                android:id="@+id/header_profile"
                layout="@layout/detail_header_profile" />

            <include
                android:id="@+id/detail_attendance"
                layout="@layout/detail_item_attendance" />

        </LinearLayout>

    </RelativeLayout>

</ScrollView>